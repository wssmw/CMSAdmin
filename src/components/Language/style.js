import styled from 'styled-components';

export const LanguageWrapper = styled.div`
    .language {
      height:100%;
      cursor: pointer;
      margin:0 10px;
      display:flex;
      align-items:center;
      justify-content:center;
      img {
        width:25px;
        height:25px;
      }
    }
`